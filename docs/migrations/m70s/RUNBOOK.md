> Historical preparation record. The authorized production transfer has now passed the final restore and all-table comparison. See [current M70 operations](../../M70_PRODUCTION_OPERATIONS.md) for the live host, services, release process, backups and remaining acceptance checks. Statements below about the Pi still serving production describe the earlier preparation stage.

# Career Dashboard: Pi to M70s migration preparation

Prepared September 2, 2026; updated after the isolated restore/build/read-only rehearsal. **Ubuntu Server 24.04.4 LTS, Node 24.20.0, PostgreSQL 17.11, and Tailscale 1.102.3 are installed. Key-authenticated SSH and passwordless administration are verified over Tailscale using `ssh m70`. The Pi still serves production; no cutover, commit, push, or production configuration change has occurred.** The September 1 backup restored successfully into an isolated database. Every archived column across all 50 tables matches the restored rows; initial application reads and watchdog detection passed. Live inspection confirmed the i5-10400, 16 GB installed memory and PNY CS900 480 GB SSD. See the dated [initial target inspection](target-host-2026-09-02.txt), [base results](target-base-result-2026-09-02.txt), and [runtime results](target-runtime-result-2026-09-02.txt). The login prompt exists in the console buffer, but its visibility on the TV remains unresolved.

## Selected outcome

Run the Dashboard, authoritative database, scheduled pipeline, and unattended ATS acquisition on the M70s with its existing 16 GB RAM. Keep interactive Claude/Codex on the Mac. Phone reads and Applied changes must work while the Mac is off. Use one writable Career Dashboard authority. Preserve all scores, status/history, receipts, downloaded pages, and submitted-resume references. Do not add a Dashboard login screen.

The Pi continues serving production until the target has passed rehearsal and a cutover is authorized. Homebridge, file sharing, and unrelated Pi services stay on the Pi. The earlier Mac-primary/Pi-replica proposal is superseded. The Pi is a rollback resource, not an automatically promoted writable replica.

## Evidence collected

See [source-postgres-2026-09-02.json](source-postgres-2026-09-02.json), generated in a PostgreSQL repeatable-read, read-only transaction. This is a live preparation snapshot, not a frozen cutover manifest.

| Observed source | Migration consequence |
| --- | --- |
| Pi: Debian 13, ARM64; app under `/opt/career-dashboard`; Node 24.19.0 / npm 11.17.0 | Use Node 24 on Ubuntu amd64. Fresh install dependencies, regenerate Prisma, and rebuild Next.js on amd64. Never copy Pi `node_modules` or `.next`. |
| PostgreSQL 17.10; about 17.6 GB (16.35 GiB); data at `/mnt/pgdata/main` on external SSD | Use a logical custom-format dump and PostgreSQL major version 17. The new SSD will contain both application and database. |
| UTF8, libc locale `en_GB.UTF-8`, source collation version 2.41 | Create the target database with the same encoding and locale. Ubuntu's libc may differ; inspect the actual target version and test restore/index/uniqueness/search behavior. |
| 49 public tables, 249 public indexes, 11 user triggers, one exclusion constraint; no invalid public indexes | Restore SQL-owned objects, including expression indexes and history triggers. Prisma schema creation alone is insufficient. |
| `pg_trgm` 1.6 and `btree_gist` 1.7, plus `plpgsql` | Verify these extensions are available before restoring. Compare installed versions and all restoration errors. |
| Additional `walking_map.Walk` table, about 32 MB | Preserve the whole database in the backup. Identify walking-map clients before changing database access. Do not shut down the Pi PostgreSQL cluster or claim that walking-map data on a restored copy is its new authority. |
| 809,169 jobs; 586 Applied; 5,773 Aim scores; 6,215 Experience scores; 130,979 legacy fit scores | Preserve these fields, job IDs, and history. Counts will change before cutover; compare the final frozen source with its corresponding restore, not today's totals. |
| Dashboard `/api/health` passed database/schema/migration checks | Existing service is healthy at inspection. This does not prove acquisition or target readiness. |
| Last listed archive: September 1, 17:44 CDT snapshot, 2.31 GB; archive table of contents readable | Suitable candidate for rehearsal after checking freshness and checksum. Not a final cutover backup and not a proven restore. |
| Seven deployment-named dumps, about 12 GB total; daily backup cron installed but no daily-named dump or backup log found | Verify scheduled backup execution and a restore before cutover. Do not infer a cause for the missing daily evidence or report backup health as proven. |
| Source privileged `sudo -n` database inspection required a password | Preparation used the application connection in a read-only transaction. Privileged source fencing, role administration, and final service control remain prerequisites; no password was requested or stored. |

## Runtime ownership

| Component | Observed now | Target and transfer condition |
| --- | --- | --- |
| Dashboard and API | Pi system service; user `j85473`; binds Tailscale IP `100.80.154.113:3000` | M70s service under the separate non-administrator `career-dashboard` account, application path `/opt/career-dashboard`, private address `100.107.116.123:3000`. Ownership and deployment changes still require rehearsal. |
| PostgreSQL | Pi cluster 17/main, database `career_db` | M70s PG17, loopback access for local services. Use SSH tunneling for Mac database administration; no public port 5432. |
| Pipeline scheduler, normalization, publication, scoring execution | Pi managed cron, every minute, filesystem lock plus database coordination | Exactly one M70s managed schedule, after final restore. Preserve schedule, provider budgets, database coordination, and current scoring behavior. |
| ATS acquisition | Saved Mac LaunchAgent requests 8 slots; LaunchAgent currently not loaded; live gate allows 8, local reserve 0; no unexpired leases at inspection | One supervised M70s acquisition process with up to the existing 8 slots. Prove actual leases/progress. Do not rename logical database worker kinds or reset acquisition history for a host move. |
| ATS repair watchdog | Mac LaunchAgent registered every 900 seconds with `--repair`; last exit 2 | Treat as a potential writer during freeze. Rehearse its existing behavior before deciding whether to enable a matching M70s timer. Carry its repair ledger; absence must not reset the cap on automated repairs. |
| Retired Mac native scoring watcher | No executable, installer, saved LaunchAgent, or loaded service; remaining logs last changed August 10 | Preserve retirement. The approved August 12 manual-scoring cutover removed it. Do not restore this historical automation as part of migration. |
| Codex ATS cutover heartbeat | Saved automation is PAUSED and contains older Pi/Mac release/capacity instructions | Keep paused. Review or retire those obsolete instructions before any future resumption; do not let them reestablish old database authority. |
| Legacy Python job project | Pi `rf_pipeline.timer` invokes `/home/j85473/job-dashboard/rf_cli.py`; service currently failed; separate SQLite backup cron | Leave unrelated workload in place. Establish whether it shares any current data or API before the freeze. Do not transplant this old scheduler as the current Dashboard scheduler. |
| Walking application | Sibling project deploy script points to `/home/j85473/walking-dashboard` and PM2; no saved user PM2 list found | Locate actual running clients and keep current behavior. Sharing a database does not authorize moving or disabling this app. |
| Interactive agent sessions | Mac | Stay on Mac. They can administer the M70s over SSH while Mac is available; Mac-off operation does not mean Mac-hosted agent sessions continue. |

## Installation record and remaining host setup

The verified installer was `/private/tmp/career-dashboard-m70s-20260902/ubuntu-24.04.4-live-server-amd64.iso` (3,405,469,696 bytes). Its SHA-256 matched Canonical's checksum list fetched over HTTPS; detached GPG signature verification was not performed. See [installer-verification.json](installer-verification.json). Joseph used the authorized Toshiba installer to replace Windows on the internal PNY SSD, selected ordinary ext4 without LVM/encryption, installed OpenSSH, and removed the Toshiba before the successful internal-disk boot. The account is `j85473`, display name Joe Lamb, hostname `m70`; Mac public-key access has been verified in separate SSH sessions. No passwords were collected.

The one-time [base preparation script](../../../scripts/migration/prepare-m70-base.sh) completed at 19:27:44 UTC through Joseph's interactive sudo session. Standard Ubuntu administration/build/SMART tools, the `en_GB.UTF-8` locale, and America/Chicago timezone are configured; clock synchronization is active. See [base results](target-base-result-2026-09-02.txt). The [SSD report](target-ssd-health-2026-09-02.txt) passed its overall health assessment with exit bitmask zero, no logged ATA errors and no reported uncorrectable errors. It reports 15,992 power-on hours and 21% endurance used. One historical interface CRC error, prior unsafe shutdowns, and a prior aborted self-test are recorded; no fresh self-test or endurance guarantee is claimed. Earlier recorded short self-tests completed without errors. Root inspection confirmed that the console buffer contains a login prompt; TV cropping/display remains the leading explanation, not a proven repaired cause.

The [runtime preparation script](../../../scripts/migration/prepare-m70-runtimes.sh) completed at 19:34:32 UTC. It installed PostgreSQL 17 and Tailscale from their signed official Ubuntu Noble repositories and the official Node 24.20.0 amd64 binary after checking its pinned published SHA-256. Node binaries are under `/usr/local/bin`; PostgreSQL listens on loopback. Tailscale enrollment is verified: `100.107.116.123`, DNS `m70.tail35f283.ts.net`. The Mac's `m70` SSH alias uses the existing SSH key and a pinned server host key. Private-network SSH works from the Mac; an away-from-home phone test remains separate. The Tailscale node key currently expires March 1, 2027 at 19:39:07 UTC. Review its expiry policy before claiming unattended long-term access. See [private access inspection](target-private-access-2026-09-02.json).

Firmware inspection through Lenovo's Linux interface reports `AfterPowerLoss = Last State`. No firmware setting was changed. An actual software reboot passed: a new boot ID was confirmed, Tailscale/private SSH and passwordless administration returned, PostgreSQL and SSH started automatically, and no failed services were reported. See [reboot verification](target-reboot-check-2026-09-02.json). The first Tailscale connection attempt timed out during startup, then succeeded; do not assume zero startup delay. Physical power removal or boot with display/keyboard disconnected was not tested.

1. Connect wired Ethernet, keyboard, display/TV with the appropriate video cable, and an installer USB of at least 8 GB. Identify the USB before writing it. Booting the installer is separate from erasing the internal SSD.
2. Proposed installer: **Ubuntu Server 24.04.4 LTS amd64**, a conservative supported baseline, not a claim that it is the newest Ubuntu LTS. Obtain the ISO and checksum list from [Canonical's release directory](https://releases.ubuntu.com/24.04/). Verify the downloaded ISO before writing the USB. Recheck availability if installation is delayed.
3. Verify the actual i5-10400, 16 GB RAM, internal SSD model/capacity/serial, and SSD SMART health. Inspect any disk contents to retain. The installation replaces Windows on the verified target SSD. Disconnect donor/external disks during installation to avoid selecting the wrong disk; there is no need to buy donor hardware now.
4. Actual hostname is `m70`, account `j85473`. The installed root is ordinary ext4 on `/dev/sda2`, approximately 446 GiB before filesystem overhead, with about 409 GiB available. The separate EFI partition is about 1 GiB. This exceeds the proposed 150 GB free preparation headroom, which is not a measured production requirement.
5. Install OpenSSH during setup. Standard unattended boot is the objective; a disk-unlock passphrase at every reboot would require a different remote-boot arrangement. Resolve that before choosing installer encryption options.
6. Set host timezone to America/Chicago and verify time synchronization. Install a trusted Mac public SSH key, test a second SSH connection, and validate SSH configuration before changing password-login policy. Keep working access during changes. Use private Tailscale access and a UniFi DHCP reservation after confirming the actual wired MAC address. Never forward SSH, PostgreSQL, or Dashboard ports publicly.
7. Check BIOS automatic power restoration; verify boot without a keyboard/display and remote access after an actual reboot. Confirm all necessary services start after network availability. Do not infer these from model specifications.

Read-only inspection, after Ubuntu is installed:

```bash
bash scripts/migration/inspect-ubuntu-host.sh
```

This script refuses the wrong OS/architecture, reports installed hardware/runtime/storage/networking, and performs no installation or service changes. Privileged SMART checks and reboot testing are separate.

## Software and paths

| Item | Proposed M70s configuration |
| --- | --- |
| Application | `/opt/career-dashboard`, managed release source, Node 24, fresh `npm ci`, Prisma generation, production build |
| PostgreSQL | PG17 from the official PGDG repository for Ubuntu Noble; explicit `postgresql-17` and `postgresql-client-17` packages, not an unversioned major upgrade |
| Database storage | `/var/lib/postgresql/17/main` on the internal SSD; verify actual cluster location rather than copying the Pi mount assumption |
| Dump retention | `/opt/career-dashboard.db-backups`, mode restricted, outside release swaps; this is an already-allowed backup location in the current activation script |
| Host environment | `/etc/career-dashboard/runtime.env`, readable only by the service account/root; include only reviewed host overrides. Source application secrets remain in restricted dotenv files, never Git or these notes. |
| Acquisition release environment | `/etc/career-dashboard/acquisition-release.env`, containing the deployed `ATS_WORKER_RELEASE_ID` generated for each release, not today's saved Mac hash |
| Web binding | New M70s private Tailscale IP, port 3000; confirm phone reachability and update the bookmark. The Pi address does not automatically transfer. |
| Local application DB URLs | Both canonical `DATABASE_URL` and runtime override must identify the target/local database. Do not leave a Pi URL hidden behind a loopback override. |

The current backup helper reads `DATABASE_URL` directly and **does not apply `DATABASE_RUNTIME_HOST`**. Leaving the old Pi URL there could create a backup of the wrong database even while the application uses the new one. Rehearsal must independently verify the database identity used by the app, cron, acquisition, migrations, and backup.

Use [PGDG's official Ubuntu instructions](https://www.postgresql.org/download/linux/ubuntu/) with Noble and major 17; verify the available patch version at installation. Start with conservative connection pools and concurrency. Do not add heavy VMs, GPU inference, or increased API parallelism to this migration. Measure memory during production-like work and builds before tuning; 16 GB is the fixed budget.

## Service drafts and release changes

The [web-service draft](career-dashboard.service.in), [acquisition-service draft](career-dashboard-acquisition.service.in), [repair-watchdog service](career-dashboard-watchdog.service.in), and [15-minute watchdog timer](career-dashboard-watchdog.timer.in) are **uninstalled templates**, not operational services. Render the private IP and validate user, paths, Node location, environment, permissions, and service syntax on the actual server with `systemd-analyze verify` before installation. The production gate is absent. A condition only prevents a future start; removing it does not stop a running service and is not a write fence. These units do not schedule the pipeline cron.

### Mac watcher and repair watchdog: explicit migration treatment

Read-only inspection on September 2 found three distinct components:

- The acquisition LaunchAgent is currently unloaded, but its saved plist has `RunAtLoad=true`, `KeepAlive=true`, a 30-second restart throttle, eight slots and a pinned release. Mac login, manual bootstrap, or the installer could reactivate it against its configured database. Freeze must fence the database as well as unload/archive its old autorun entry.
- The repair watchdog remains registered every 900 seconds with `RunAtLoad=true` and `--repair`. Its last logged check at 19:45:01 UTC reported `no_live_workers`, returned critical exit 2, and applied no repairs. It does not restart the acquisition process; it can reopen wrongly blocked provider circuits and resume deferred batches. Those are database writes and must be stopped/drained during freeze.
- The old native scoring watcher is retired. Its August 10 log files are historical, not a running process. The [approved August 12 implementation record](../../CAREER_DASHBOARD_AIM_EXPERIENCE_SCORING_IMPLEMENTATION_LOG_2026-08-12.md) documents removal. The migration must preserve that behavior.

The watchdog caps each repair action at three attempts per six hours using `data/runtime/ats-watchdog-repairs.json`. The file was absent during this inspection; do not silently substitute an empty history at activation. At the final freeze, record the source ledger's presence/checksum and recent repair evidence. Copy a present ledger with service-user permissions, or explicitly establish a truly absent history before initializing it. Preserve this file across release swaps and rollback. The new draft refuses a missing or malformed ledger and requires a separate repair-enable marker; neither marker has been created. Preserve the existing repair logic and cadence, surface critical exit 2, and verify detection against the isolated restore before enabling repairs. Detection-only execution has now passed against the SELECT-only rehearsal database. The same service account must have access to its production database and required runtime files without sudo before activation.

Cutover must stop the Mac watchdog's scheduling and any in-flight invocation, then start exactly one M70 watchdog timer after restore/verification. A Mac wake/login must not recreate a second repair owner. Deployment must stop/drain the watchdog alongside acquisition before a release swap and restore only the deliberately enabled units afterward. A saved paused Codex cutover automation also contains superseded Pi/Mac instructions; it stays paused during migration.

Required release integration work before cutover:

1. Parameterize all production target references together: workflow Tailscale ping, SSH host-key verification, user/key, deployment host, backup directory, and database data directory. Current `.github/workflows/deploy.yml` still automatically deploys pushes to `main` to the Pi. Do not push a migration-preparation commit through it unintentionally. Enroll the M70s CI identity and verify its SSH fingerprint before retargeting.
2. Bootstrap the target account, restricted configuration, initial release, and services separately. The existing deploy script expects an installed app and service; it is not a bare-Ubuntu installer. Its unattended sudo mechanism also needs a concrete target configuration, not a promise of permissionless administration.
3. Add acquisition supervision to the deployment lifecycle: stop and drain the M70s acquisition process before swapping its application directory, install the matching release identifier, health-check, then restart it. The existing deploy script manages the web service and cron but not this new system service. Reusing dependencies must account for architecture; on the initial migration build everything fresh.
4. Launch the portable acquisition TypeScript entrypoint under systemd rather than the Mac wrapper. The wrapper deliberately rejects a loopback database. Keep the existing logical lane name and preserve the live 8-slot global gate and zero Pi reserve. Review the current rollout profile's environment overrides so deployment cannot silently change the intended capacity.
5. Preserve persistent files deliberately. Inventory restricted dotenv files, submitted or generated resume references, scoring exports/results/archive, and runtime control ledgers. The canonical resume is tracked with the release: do not overwrite it with an older Pi file. Preserve historical runtime files separately; do not blindly restore live filesystem locks or stale process state. Existing release staging copies daily cron logs, not every persistent file.
6. Prepare cron and daily backups for the verified M70s base URL and backup directory. Prove a scheduled backup and independent off-host copy; local dumps on the same SSD do not protect against loss of that SSD. The Pi can retain a restricted backup copy without becoming a second writable Dashboard authority.

## Restore rehearsal: target only, before production changes

1. Keep Pi production running. Transfer a complete custom-format dump over SSH into a restricted M70s directory, using `umask 077`. Record SHA-256 and verify after transfer. Do not include database dumps or secrets in Git. The existing archive can establish a first rehearsal; refresh it as needed without pretending it is a final snapshot.
2. Inspect archive contents, create a clearly named **rehearsal** database from template0 with UTF8 and `en_GB.UTF-8`, and establish the necessary owner/extension permissions. Backups omit global roles/passwords and ACLs; recreate only required access deliberately. Inventory database/role settings that are not restored by a plain restore into an existing database.
3. Use PG17 `pg_restore --exit-on-error --no-owner --no-privileges` against that explicit rehearsal database, with moderate parallelism (initially 2). Never use a command containing the source Pi endpoint as a restore destination. An interrupted/failed parallel restore leaves a partial rehearsal database; recreate that disposable database before retrying. Do not treat nonempty tables or an archive listing as success.
4. Restore all schemas and SQL-owned objects, not just Prisma tables. The whole-database backup includes walking-map data; do not start a second walking app against its copy. Compare public indexes, triggers, constraints, extensions, migrations, and any other restored objects against archive/source evidence. Logical restoration rebuilds indexes using target locale behavior. Stop on duplicate-key, collation, missing extension, or SQL-definition errors; do not merely refresh collation metadata to suppress a warning.
5. Run `ANALYZE` on the rehearsal database, then validate exact row counts, IDs, statuses, existing score values, histories, and acquisition receipts against a corresponding consistent source snapshot. A fresh source report and an older dump will naturally differ. For final verification, freeze source writers before both dump and manifest. Row counts alone cannot prove field preservation; compare hashes/exports of scored fields, application status/history, and other retained content as well.
6. For the first health/read checks, use a restricted database role and an isolated local port. For write tests, use only the disposable restored database. Keep production credentials/provider access, cron, acquisition, and repair timers disabled or isolated; ordinary page requests must not accidentally run background production work. Test Applied/bookmark changes, history, scores, resumes, Stats, and search on the rehearsal copy. Never run reset/fresh-start or score-invalidation commands for migration.
7. Record elapsed dump/transfer/restore/build times, storage and memory peaks, representative query latency, and any remaining errors. Use those measurements to estimate the production maintenance window. No downtime estimate is proven yet.

Refresh database inventory without deploying the script to the Pi:

```bash
ssh -o BatchMode=yes -o ConnectTimeout=8 j85473@192.168.1.208 \
  'cd /opt/career-dashboard && node scripts/with-env.mjs node --input-type=module' \
  < scripts/migration/inventory-postgres.mjs > /tmp/pi-preparation-inventory.json
```

For a frozen source or isolated restored copy, the optional `--include-row-counts` flag counts every user table. Invoke the installed script through `scripts/with-env.mjs` on that host. It uses a one-connection pool, read-only repeatable-read transaction, query/lock timeouts, and suppresses raw errors/connection secrets. It is a comparison aid, not a data-integrity certificate or automatic cutover tool.

## Authorized cutover sequence, to perform later

1. Require a passing rehearsal, working private phone/SSH access, tested source administration, complete writer inventory, validated deployment changes, and a measured maintenance window. Freeze release changes so both machines use the same chosen application commit.
2. Announce a short Dashboard outage. Pause/stop the current scheduler and in-flight processing gracefully. Stop the Pi web/API and every Career Dashboard writer, including any Mac acquisition, watchdog, import, developer process, or automation. Disable their automatic restart paths. The saved acquisition plist must remain unable to restart against the old database when the Mac later wakes.
3. Establish an effective source write fence and prove zero active writers/transactions and released or expired leases. Identify the walking-map clients first: do not stop their database cluster or indiscriminately revoke shared credentials. Resolve access/role separation where necessary. A drained queue, expired lease, or stopped web service alone does not prove every writer is stopped. No reset, cancellation of retained work, or historical score changes are part of this host move.
4. With Career Dashboard writes frozen, make the final dump, exact source manifest, and persistent-file copy. Keep source frozen through restore. Verify archive checksum at destination. Any shared walking-map writes must have an explicit treatment so their snapshot is preserved without falsely making its copy authoritative.
5. Restore into a fresh target production database. Match all Career Dashboard counts/IDs/values/history and SQL-owned objects to the final source before enabling public access or acquisition. Reconcile interrupted operational leases through normal expiry/recovery; never erase acquisition evidence. Replace rehearsal changes with the final source data.
6. Retarget the single deployment path and all application consumers to the M70s. Render/start the production web service, verify database identity and health, and test phone access. Activate exactly one scheduler and acquisition process only after checking their release and database identity. Preserve existing scoring settings and provider budgets. Keep old Pi Dashboard writers stopped/fenced.
7. Verify the Mac can be turned off while the phone still browses and makes an Applied change that survives refresh and server restart. Confirm both query responsiveness and actual acquisition/publication/persistence progress. Prove daily and off-host backups and a restore. A health endpoint alone is not acceptance.

## Rollback and completion

- **Before M70s accepts new writes:** stop/fence all target writers, verify there were no accepted target changes, restore original Pi routing/services and only the originally enabled automation. Confirm a single writer again.
- **After M70s accepts new writes:** do not simply turn the stale Pi Dashboard back on. Stop/fence target writes and transfer/reconcile the newer authoritative state back to the Pi, preserving shared walking-map data as well, then validate before resuming. Application-directory rollback alone cannot recover database changes.
- Retain the Pi data and the verified final dump until acceptance and backup restoration are demonstrated. Do not repurpose or erase the Pi SSD as part of preparation.

Runtime installation completed at 19:34:32 UTC and was independently verified: Node 24.20.0, npm 11.19.0, PostgreSQL 17.11, and Tailscale 1.102.3. PG17 listens only on 127.0.0.1:5432; both required extensions are available. Tailscale enrollment and private SSH are verified. The cluster originally contained only postgres/template databases; a separate rehearsal database has now been restored and compared with its archive. See [runtime results](target-runtime-result-2026-09-02.txt) for the dated pre-restore inspection.

Joseph authorized passwordless administration. The [helper](../../../scripts/migration/enable-m70-admin.sh) completed, and fresh SSH sessions verified `sudo -k -n id -u` returns zero. The root-owned mode-0440 rule and complete sudo configuration validate. Every process/session running as `j85473` has full sudo capability, as explained and accepted by Joseph. No password is stored or embedded. Revoke via `sudo rm /etc/sudoers.d/99-m70-admin`.

**The dedicated service account is created and verified unable to use sudo.** It has only its own group and a disabled login shell. Dormant web/acquisition/watchdog templates use that account. Deployment must grant it read access to code/restricted configuration and write access only to required data/log/runtime paths, use its own database access, and preserve these permissions during release swaps. Existing deploy ownership/cron assumptions must still be changed and rehearsed before activation. Creating the account alone does not satisfy application runtime permissions.

Remaining gates: production permissions and scheduler/deployment integration; optional TV display correction; private access away from home and Tailscale expiry policy; physical power recovery/headless-boot verification; privileged source control; walking-map/legacy-writer dependency confirmation; application write/pipeline/backup rehearsal and browser dependencies; final frozen source manifest/restore and authorized cutover; phone/Mac-off acceptance.

## Completed restore and application rehearsal

- [Restore results](target-restore-result-2026-09-02.txt): all 50 user tables restored, 249 public indexes, 11 user triggers, one exclusion constraint, zero invalid indexes. Restored database is about 8,044 MB. PostgreSQL rebuilt indexes with target libc collation 2.39, matching its actual installed version; source metadata reported 2.41. Representative sort/search behavior still needs functional acceptance.
- [Field-level comparison](target-row-comparison-2026-09-02.json): the verified archive was decoded into COPY rows and every archived column compared with the target's COPY output using sorted SHA-256 row digests. All 50 tables match, including jobs, scores, status/history, acquisition evidence, migrations and the walking-map snapshot. This does not compare schema definitions or sequence state, and does not make the copied walking application authoritative. The historical dump contains 736,525 jobs and 567 Applied; their difference from today's source is expected, not data loss.
- [Read-only application checks](target-application-read-checks-2026-09-02.json): HTTP 200 for health, Dashboard, Applied and bookmarked lists; health confirms database/schema/migration checks. The API sees the expected historical Applied/bookmarked totals. The temporary server bound only 127.0.0.1:3300, ran as `career-dashboard`, used a SELECT-only rehearsal account and could reach only localhost. It had no provider credentials and was stopped after the checks. No write or pipeline endpoint was exercised.
- [Watchdog detection](target-watchdog-detection-2026-09-02.txt): the existing watchdog executed on Ubuntu against the restricted rehearsal account without `--repair`. It correctly reported no live acquisition leases in the inactive snapshot and applied no repairs. This proves detection can run; it does not prove healthy production acquisition or authorize activation of repair scheduling.
- Rehearsal credentials were generated locally on the M70, stored in `/etc/career-dashboard/rehearsal.env` with root/service-group access, and never printed or copied into Git. The account has SELECT but lacks UPDATE/DELETE on jobs and defaults to read-only transactions. It is not the production service credential.

## Preparation verification performed

- Database inventory script ran successfully on the live Pi with `transaction_read_only=on`; it captured all 50 user-table metadata entries, public schema objects, migration bookkeeping, and current job status/score counts. The optional full-table row-count mode was not run against production.
- JavaScript and shell syntax checks passed. Invalid arguments, invalid connection URL, and wrong-host guards returned their intended errors without opening a database connection or changing a host.
- Prepared documentation links and whitespace checks passed. All four service/timer drafts were rendered in a review-only directory and passed `systemd-analyze verify` on the M70. They are not installed or enabled; runtime credentials, ownership and behavior remain separate checks.
- The September 1 archive transferred with matching SHA-256 `084f885ba22d4793a5a27d0abbe51e796f8f8a55a8c0aca377d7416589d49889` and size 2,312,598,898 bytes. The isolated restore started at 19:47:56 UTC; restoration took 745 seconds and post-restore analysis/inventory completed at 20:00:43 UTC. All 50 tables then matched the archive at field level. This proves preservation of that older snapshot, not equality with today's live Pi.
- Installer SHA-256 matched the official downloaded checksum entry. The Toshiba media was written, read-back verified, and safely ejected; Joseph subsequently completed installation onto the M70s internal SSD. Successful SSH inspection confirms the installed system, actual hardware, runtimes, and private-network access.
- A frozen export of application commit `340dbdd0e8ba923bbeef73fb98f607375c0ba3d5` passed a fresh `npm ci`, Prisma generation and Next.js production build under the non-administrator service account, 19:50:15–19:51:29 UTC (about 74 seconds for the whole script). Its temporary build URL points to unused local port 1; no source credentials were copied. This does not activate the Dashboard or validate browser acquisition dependencies. See [build results](target-build-result-2026-09-02.txt).

Technical references: [PostgreSQL logical dumps](https://www.postgresql.org/docs/17/backup-dump.html), [portable archive restoration and flags](https://www.postgresql.org/docs/17/app-pgrestore.html), [collation behavior](https://www.postgresql.org/docs/17/collation.html). External references describe the procedure; the dated inspection and rehearsal artifacts above provide execution evidence.
